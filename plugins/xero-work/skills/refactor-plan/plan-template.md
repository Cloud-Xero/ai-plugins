# リファクタリング計画書テンプレート

`refactor-plan-writer` はこの構成に従って `docs/refactoring/<対象>.md` を書く。
`<>` は差し替え箇所、丸括弧の注釈は書き方の指示（出力には残さない）。節の順序と番号は変えない。分量は対象の規模に応じて増減してよいが、**節を省略しない**。

---

```markdown
# <対象> リファクタリング計画

> 親計画: [<親ドキュメントのパス>](<相対パス>) の **<Phase N（見出し）>** を実行可能な粒度に落とし込んだ詳細計画。
> 原則: **全 PR 挙動不変**。1 PR = 1 つの構造変更。抽出順序は「型 → 純関数 → カスタムフック → プレゼンテーション部品」。

（親計画が無ければ 1 行目を省き、原則の行だけ残す）

---

## 1. 現状分析

### 1.1 ファイル一覧（実測: `wc -l`、<計測日> 時点）

| ファイル | 行数 | 内容 |
|---|---:|---|
| `<path>` | <n> | <役割。既に理想形なら「変更不要」と明記> |

（実測値のみ。合計とディレクトリ構成の有無（`components/` `hooks/` `lib/` が存在するか）も 1 行で書く）

### 1.2 <主要ファイル> の内部構造（grep 実測）

トップレベル定義 **約 <n> 個**（型 <n> / 純関数・定数 <n> / コンポーネント <n> / context <n> / フック <n>）。

| フック / 呼び出し | 回数 |
|---|---:|
| `useState` | <n> |
| `useMemo` | <n> |
| `useEffect` | <n> |
| `useCallback` | <n> |
| データ層直叩き（`supabase.` 等） | <n> 箇所（内訳） |

行レンジ別の主要ブロック（開始行は grep 実測、行数は次定義との差分）:

| 行レンジ | 定義 | 概算行数 | 責務 |
|---|---|---:|---|
| <a>–<b> | `<名前>` | <n> | <責務> |

（対象が複数ファイルの群なら、この節を「ファイル間の重複（実測ベース）」に置き換える。同一パターンが何ファイルに何回複製されているかを表で示す）

### 1.3 責務の混在（1 ファイル内に同居しているもの）

1. **ドメインロジック**: <具体名>
2. **データ層**: <テーブル / API と箇所数>
3. **<以下、対象に応じて>**: DnD / フォーム / Markdown / UI プリミティブ / URL・ナビ状態 …

### 1.4 外部依存（分割時に壊してはいけない境界）

- <対象を import している外部ファイル、対象が import している共有モジュール、Server/Client 境界、Context の授受>

---

## 2. 問題点

### 2.1 保守性
- <規模が grep / diff / レビューの限界を超えている根拠を実測値で>
- <近似コピーの重複と、それが誘発する「片方だけ直る」事故>
- <feature 越境 import・逆流依存>

### 2.2 テスト容易性
- <既存テスト本数（0 なら 0 と書く）。純関数が module-private で import できない等、テストを阻む構造要因>
- <モック境界の不在>

### 2.3 パフォーマンス（該当する場合のみ）
- <再描画の連鎖、`React.memo` 不在、重い依存の静的 import、派生値の setState 連鎖>

---

## 3. 目標構造

```
<分割後のディレクトリツリー。各ファイルに「# 役割 (~n 行)」の見積コメントを付ける>
```

最大ファイル目標: **<n> 行以下**（中間状態で一時的に超過してよい範囲も書く）。
<変更しないファイル（既に薄い Server Component 等）を明記>

---

## 4. 実行ステップ（PR 単位・番号順・各 PR 挙動不変で独立マージ可能）

依存関係: PR1 → PR2 → …（並行可能な PR は明示）。<逐次実行を推奨するなら理由（巨大 diff のコンフリクト回避）も書く>

### PR1: 安全網 — <テスト基盤導入（未導入時のみ）> + characterization テスト
- <テスト基盤が未導入なら、追加する devDependency・setup ファイル・設定変更を具体名で>
- <モックヘルパーの新設パスと、モックするチェーン>
- <smoke テストで固定する挙動を 3〜5 個、具体的に>
- **コード変更ゼロ**。以降の全 PR でこのテストが緑であることをマージ条件にする。

### PR2: 型の抽出 → `<path>/types.ts`
- <移設する型名を列挙>。呼び出し元は re-import。
- 依存: なし（機械的移動）。ロジック差分ゼロ。

### PR3: 純関数の抽出 → `<path>/lib/` + 単体テスト
- `<file>.ts`: <関数名を列挙。副作用を伴うものは純関数と分離することを明記>
- 各ファイルに単体テスト（<テストする分岐>）。**費用対効果が最大の PR**。

### PR4 以降: <UI プリミティブ / フック / ビュー分割 / 共通化 / 仕上げ>
（1 PR につき: 何をどこへ / なぜ安全に切れるか / 依存する PR / 追加するテスト、を 3〜5 行で。最後の PR は「薄いコンテナ化 + dead code 掃除 + 完了基準の実測」にする）

### PR<最終>: 仕上げ
- <エントリポイントをフック合成 + レイアウト JSX のみ（≤ n 行）に>
- 未使用 import・dead code の掃除。
- 完了基準の実測: `wc -l` で全ファイル ≤ <n>、`<test コマンド>` 緑、`<build コマンド>` 通過。
- **本計画書（`docs/refactoring/<対象>.md`）自体をこの PR で削除する**。

---

## 5. テスト戦略（<テストランナー> + <RTL 等>）

### smoke（characterization）で固定するもの — PR1 で先行作成
- <描画・代表操作・分岐切替。jsdom で実駆動できないもの（DnD 等）は「ハンドラ直接呼び出しで代替」と明記>

### 分岐を網羅的にテストするもの（単体テスト）
- `<lib/xxx.ts>`: <境界値・全分岐を具体的に>
- `<hooks/xxx.ts>`: <楽観更新の state 遷移・失敗時 rollback・呼び出し検証>

### モック方針
- <データ層 / ルーター / 状態ストア / 重い依存 それぞれについて、モックするか実体を使うか>

---

## 6. リスクと注意点

### 6.1 プロジェクト制約由来（CLAUDE.md / メモリ）
- <本リファクタで直接抵触しうる制約を、原文の趣旨を保って列挙。「抽出時に『改善』しないこと」の形で書く>

### 6.2 技術リスク
- **循環 import**: <一方向依存の順序を明記>
- <テスト環境固有の落とし穴（portal + jsdom、Context 必須コンポーネント等）>
- <git 履歴の追跡性: 「移動のみ」「編集のみ」のコミット分離>
- **ブランチ運用**: 着手前にユーザーに確認する。
- **検証範囲**: 各 PR は lint / typecheck / test / build まで。dev サーバー起動・目視確認はユーザーに委ねる。

### 6.3 実装前に相談が必要な分岐
- <共通化の粒度、dead code の削除可否、逆流 import の解消方法など、勝手に決めてはいけない判断を列挙>

### 6.4 スコープ外（やらないこと）
- <挙動変更全般、状態管理・境界の見直し、スキーマ変更 …>

---

## 7. サマリ

| PR | 内容 | 規模感 | 依存 |
|---|---|---|---|
| PR1 | <内容> | <+n 行 / 変更なし> | — |

<全体で PR n 本。完了時の最大ファイル行数 <n>、テスト <n> 本、といった着地点を 2〜3 行で>
```
