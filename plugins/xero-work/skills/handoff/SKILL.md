---
name: handoff
description: いまのセッションが壊れて（ツール結果が固定文字列を返す・出力が得られない・コンテキスト飽和など）新しいセッションを立ち上げざるを得ないときに、現在のセッションから次セッション用の引き継ぎを簡潔にまとめてターミナルに出力する。ファイルには保存せず、そのままコピペして新セッションの最初のメッセージに貼れる形で出す。「引き継ぎを作って」「ハンドオフして」「新セッションに渡す内容をまとめて」と言われたとき、または /handoff で呼び出されたときに使う。作業ログをファイルに残すのは harvest スキルの領分。
argument-hint: '[引き継ぎに含めたい補足があれば]'
---

@INSTRUCTIONS.md
